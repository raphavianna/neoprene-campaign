# Histórico de versões do master prompt

Esta pasta arquiva versões anteriores do `CLAUDE.md`. O master prompt vigente nunca é apagado ou sobrescrito sem cópia aqui.

## Estado atual

**Vazia.** A v1 do master prompt não existe neste repositório — o histórico git começa num commit único (`ef93043`, "Add files via upload") que já traz a v2.0. Não havia arquivo v1 para arquivar, então nada foi descartado.

Se a v1 for recuperada de fora do repositório, salvá-la aqui como `master_prompt_v1.md` sem editar o conteúdo.

## Convenção

`master_prompt_v<N>.md` — cópia literal da versão substituída, arquivada no mesmo commit que promove a versão seguinte.
